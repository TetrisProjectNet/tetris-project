import { ShopItemFilterPipe } from './shop-item-filter.pipe';

describe('ShopItemFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopItemFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
