import { EmailHiderPipe } from './email-hider.pipe';

describe('EmailHiderPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailHiderPipe();
    expect(pipe).toBeTruthy();
  });
});
